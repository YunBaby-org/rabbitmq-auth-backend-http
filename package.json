{
  "name": "rabbitmq-authentication-backend-http",
  "version": "0.0.0",
  "description": "The http authentication backend for internal RabbitMQ",
  "main": "build/src/index.js",
  "types": "build/src/index.d.ts",
  "files": [
    "build/src"
  ],
  "license": "none",
  "keywords": [],
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "check": "gts check",
    "clean": "gts clean",
    "compile": "tsc",
    "fix": "gts fix",
    "prepare": "npm run compile",
    "pretest": "npm run compile",
    "posttest": "npm run check"
  },
  "devDependencies": {
    "gts": "^2.0.2",
    "typescript": "^3.8.3",
    "@types/node": "^13.11.1"
  }
}
